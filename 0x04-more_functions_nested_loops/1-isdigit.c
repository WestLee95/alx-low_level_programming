#include "main.h"


/**
 *_isdigit - Checks if character is a digit
 *ranging from 0 to 9
 *@c: character to be checked
 *
 * return: returns 1 if c is digit, 0 if otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
